/** Renders a deep walnut sill, sized to support future plants and decor. */
export function WindowSill() {
  return (
    <div
      className="relative -mx-[3%] h-5 rounded-b-md bg-gradient-to-b from-[#8a5a3e] to-[#4b2c20] shadow-[0_8px_10px_rgba(18,8,6,0.35)] sm:h-7"
      aria-label="Window sill"
    />
  )
}
